# AiOfficerActionDispatch state machine $C0BB-$C982 in prg_08_09.asm

- **Category:** project_introduction
- **Memory ID:** (ID not exposed by tooling; b228bd46 belongs to BattleStatusPanelDraw)
- **Keywords:** AiOfficerActionDispatch, state machine, battle officer, formation, prg_08_09, dispatch table, inline jump table, $C0C1

## Content

In asm/banks/prg_08_09.asm, $C0BB-$C982 is fully analyzed and restructured into `.proc AiOfficerActionDispatch` (byte-exact verified, tools/tmp_verify_c0bb.py harness, 2248 bytes 0 mismatches). Entry via JMP stub $A009. Dispatches on $0501 via B1F_CallbackDispatcher with inline 10-entry table (states 0-9): State0_ShowActionPanel, State1_GrowStatA, State2_GrowStatB, State3_CheckOfficerStat, State4_ConsumeAndRestore, State5_SetupFormation, State6_RenderFormationSprites, State7_ApplyTileEffect, State8_WaitForNextCommand, State9_RouteNextAction. Nested bare-name helpers: ExpandFormationSlots ($C851, ALSO a bank entry via $A01E stub, referenced externally as AiOfficerActionDispatch::ExpandFormationSlots), CheckSpecialTiles ($C73B, carry SET=apply tile normally / CLEAR=skip), GetBattleSideOffset ($C93E, X=0 near/2 far by $0504 sign, called externally from $C983+ via scoped name), CheckAnimQueueDone ($C948), DrawActionMarker ($C95A). Side stat pairs: $0522/$0523=stat A, $0526/$0527=stat B (clamped 0..9999=$270F). $0500=command id, $0501=phase, $0081=command-step flags (bit0 execute, bit1 confirm). Data tables: State0/4/5/7/9PanelLayout, FormationIdTable ($C525, 30 bytes), SlotParamTable ($C61E, 8 words), TileOffsetTable ($C89E, 32 words incl. former garbage $C8B8-$C8DD), FormationTileLayouts ($C8DE, 96 bytes=6 formations), ActionMarkerSprite ($C97E). CRITICAL FIX: inherited State 6 ($C543-$C61D) was corrupted/truncated — the true ROM code has an 18-byte prefix (LDY #$30 / JSR B1F_SwitchBank8_B / zero $0010-$0012) plus a first-slot cache-init path with JMP and a duplicated slot-pointer setup; restored to match ROM exactly. The former $C8B8-$C93D illegal-opcode block was data (TileOffsetTable continuation), not code.
