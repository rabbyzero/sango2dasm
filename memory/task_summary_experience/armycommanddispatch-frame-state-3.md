# ArmyCommandDispatch frame state 3 decoded in prg_1b_1c.asm ($ADF2-$B758)

- **Category:** task_summary_experience
- **Memory ID:** acb52320-533c-45a4-983a-13a9d341043b
- **Keywords:** ArmyCommandDispatch, army command screen, frame state 3, CallbackDispatcher sub-states, sortie conscription reconnaissance governor, byte parity verification

## Content

ArmyCommandDispatch decoded and refactored in prg_1b_1c.asm ($ADF2-$B758, MapScreenFrameStateTable state 3, the 軍隊 army command screen; manual pp.24-25). Structure: one .proc with a 25-entry B1F_CallbackDispatcher inline .word table indexed by $0401. Menu item order 0=Sortie(出陣), 1=Conscription(徴兵), 2=Reconnaissance(偵察), 3=GovernorAppointment(任命) derived from selection routing, not the manual's prose order. Key decoded mechanics: ConscriptionApply ($B36D) implements 金20/100人 pricing (entered amount $048E/F: gold rec+$02 -= n*20 via $EBE9 mult, soldiers rec+$0C += n*100); Reconnaissance hands off to frame state $0C province data screen via frame 0/sub 2 (MapRulerIntroWait); GovernorAppointment swaps the selected officer into the province roster head ($B708) unless the ruler is in the province (($EE+1)==$0402, monarch auto-governor); sortie member menu requires >=2 members and the ruler ($EE+0) heading the member list $0481; MarchRouteGate ($B5A6) routes to result pan ($0473 negative), peaceful march $B670 ($0507 low nibble == 7), or war scene via B0C_0D_ExchangeSceneInit_Entry stub $A003 (LDY #$2C) when $0087 bit7 set. Fixed five misclassified code pockets: $B0CB-$B106 (SortieProvisionDialog code), $B232 (JSR $D7A8), $B25A-$B277 (.word $A015 bank-$19 undocumented stub -> JMP $B7D9 + code), $B5C6-$B5CD (.word B1D_1E_ImmediateOverlay + code), $B61D-$B62A (.word B0C_0D_ExchangeSceneInit_Entry + RTS + ReconMapScrollStep head). Labeled inline menu/cursor tables at $AEFF/$B0C2/$B20E/$B585. ~120 Loc_ labels renamed semantically; all branch/JSR/JMP operands symbolized. Per user feedback, 74 handler-internal labels were then converted to @-locals scoped to their parent sub-state region; only the 25 dispatch-table targets plus three cross-handler targets (SortieProvisionSeed $B08C from sub 3->4, SortieMarchDialogSetup $B235 from sub 6->8, ReconPromptWaitExit $B479 shared by subs 14/15) stay global. Region delimiters are the remaining global labels including interleaved data-table labels. Instruction comments uniformly re-padded to the col-42 convention. Verified byte-exact with tools/verify_1b_1c.py: 16384 bytes, 0 mismatches. Note: full `make` currently FAILS at HEAD from pre-existing Loc_ symbol collisions between prg_19_1a.asm and prg_1b_1c.asm bank-$1C regions (e.g. Loc_CEC7) — use the per-bank harness for this bank until that is resolved.
