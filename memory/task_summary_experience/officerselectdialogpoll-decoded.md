# OfficerSelectDialogPoll dialog system decoded and renamed at $D64A in prg_1b_1c.asm

- **Category:** task_summary_experience
- **Memory ID:** a156380b-cc85-4c16-a16c-2cab7ad450b3
- **Keywords:** OfficerSelectDialogPoll, DialogWindowClose, officer roster dialog, troop pool, prg_1b_1c

## Content

OfficerSelectDialogPoll ($D64A-$D74B) decoded and renamed in prg_1b_1c.asm. Shared strategy-mode officer roster dialog poll over the map-screen overlay. Mode byte $047C: $0F = single pick (roster officer id from $0151+row into $0481, close $80; picking the country ruler via $DD4F latches $81 and keeps the window open); $01-$0E = multi pick (free-slot counter for $0481-$048A, $FF = empty; A toggles row and repaints mark tile; Done row at index $047B compacts the list and closes $81); $10 = tail-jump to troop pool distribution; B closes $90 = cancel. Persistent cursor row = menu_cursor_page ($0425, step-1 menu via B1F_MenuStep1); row limit $047B; selected row echoed to $047A.

Sub-procs created: OfficerSelectRowMarkDraw ($D773, queues tile $3E mark / $01 blank + attr $80 at PPU $24A2 + row*$40 via B1F_MathMul24x8 with $0040 stride); DialogWindowClose ($D7A8, shared close step with 10 external callers — clears $008F, sets window render params $0061=3/$00B2=0/$00B3=5/$00B4=9, dead read of $00C2); OfficerTroopPoolAssignPoll ($D7C5-$D986, phase $10: pad bit7 Right gives 100 men from pool $042C/$042D cap $2710 to row officer TroopCount at record +$08 capped 1000 via $03E7 precheck, pad bit6 Left returns 100 while pool <= $26AC; both run TroopPoolRemainderSweep first; redraws row count at $24BB + row*$40 and pool at $273A through @BcdDigitsDraw/@DigitTileWrite with digit tiles $76+digit and leading-zero blanking via $000A); TroopPoolRemainderSweep ($D987, B1F_MathDiv16 by 100: rounds TroopCount down to multiple of 100, sweeps remainder into the pool, returns spill above $2710 to the officer).

Data tables named: OfficerSelectMenuTable ($D74C, MenuStep1 rows $00-$0A + $FF), OfficerSelectCursorPosTable ($D758, 11 OAM Y/X pairs at X=$08, Y=$27..$C7 step $10), OfficerSelectCursorSprite ($D76E, dY/tile/attr/dX + $80 terminator). Callers: 12 JSR sites in-bank (CastleDev, CastleMove, Sortie, ResultPan, Governor, Hospital, Academy, Armory, Intrigue states 3/9/10) + stub $A006 (JMP target of B19_1A dispatch word at $BEE5). Pad bit order confirmed from B1F MenuCursorSystem: bit0=A, bit1=B, bit4=Up, bit5=Down, bit6=Left, bit7=Right. Verified byte-exact with tools/verify_1b_1c.py (0 mismatches) and tools/verify_19_1a.py. SearchReplace quirk: large multi-hundred-line replacements report spurious "save file failed: unknown" but DO apply — always re-grep after the error instead of assuming failure; replace_all on this file can genuinely fail to match while per-site unique replacements succeed.
