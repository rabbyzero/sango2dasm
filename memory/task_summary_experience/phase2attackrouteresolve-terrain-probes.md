# Phase2AttackRouteResolve and terrain probes decoded in prg_0e_0f.asm ($C30F-$C826)

- **Category:** task_summary_experience
- **Memory ID:** 289cb445-d94e-4c49-aea6-74294d2efb7c
- **Keywords:** Phase2AttackRouteResolve, arrow path tile check, terrain passability, long range gate, flight counter, byte verification

## Content

Refactored Loc_C30F in asm/banks/prg_0e_0f.asm via the Verify/Replace/Rename/Fix/Explain workflow. Loc_C30F is Phase2AttackRouteResolve (.proc $C30F-$C826): ranged attack-route resolver called from Phase2ActionGate ($A536, column action bits == 2). Randomized axis (B1F_RandomByte bit0) and side (sign bit); four chains (left/right/up/down) scan distances 1-6. Every tile runs Phase2ArrowPathTileCheck ($CAC8, bounds + terrain) then Phase2StepTileProbe. An enemy at distance 1 aborts the side (point-blank belongs to the move route); empty/ally tiles are shot over; the first enemy commits $054D = target column, $0548 = flight counter $1C/$2C/$3C/$4C/$5C (distances 2-6; Phase2AttackArrowAnim decrements by 2 per frame), $0549 = direction 0 up/1 down/2 left/3 right. Distances 5-6 are gated by the acting side's $0575 nibble (loaded to 3 by Phase8RowCounter575, ticked by BattleSideStatusCountersDecrement). Failure dispatch on $0010 mirrors Phase2MoveRouteResolve (3 = give up $0549=$FF, 1 = switch axis, else flip side); the @LeftFail self-loop quirk makes a fresh left failure skip the right side. Helpers renamed: $CAC8 = Phase2ArrowPathTileCheck (terrain classes 4/5 forced passable, arrows fly over); $CAF9 = BattleTerrainPassabilityCheck (tile id to class via BattleTerrainClassTable $CB59, 144 entries; class 0 passes, classes 1/5 always block, 2/3/4 per troop type via BattleTroopTerrainTableA $CBE9 / TableB $CBED; troop type = officer record byte $0B bits 2-3 of side lead $0560/$0561; map grid via $0544, pointers $BB1E, bank $BB48 + B1F_SwitchBank8_B). Verified byte-exact via tools/verify_0e_0f.py (16384 bytes, 0 mismatches). Full make still fails from pre-existing unrelated symbol clashes (prg_17_18 vs prg_0c_0d).
