# OfficerCardAnimStep $B2D7 verified and entry stub renamed in prg_19_1a.asm

- **Category:** task_summary_experience
- **Memory ID:** 4e945286-195f-4565-9715-011d08d23dae
- **Keywords:** OfficerCardAnimStep, CardFillDispatch, flat procs, no nesting, card animation

## Content

prg_19_1a.asm card-carousel region uses three flat top-level .procs, no nesting: ProvinceOfficerRosterDispatch ($AFE5-$B2D6, roster sub-states + CardAnimWait/RosterScroll/PadEdgeCursorMove), OfficerCardAnimStep ($B2D7-$B4BE incl. private CardAnimPatternPtrs/@CardAnimPatternFrames/CardRowBaseTable), CardFillDispatch ($B4BF-$B7D8 incl. all frame handlers, DigitStore*/NamePlate* helpers, portrait tile tables). Cross-proc references resolve as plain names: JSR CardFillDispatch from OfficerCardAnimStep, JMP OfficerCardAnimStep from the OfficerCardAnimStep_Entry stub at $A012 (top-level proc names are global symbols). User preference: avoid nested .procs in this project; end the parent proc before the next function instead. Verified byte-exact with tools/verify_19_1a.py, 16384 bytes 0 mismatches. OfficerCardAnimStep: card slide-in animation gate + step; builds 32-byte strip record $0380-$03A3 (count byte $20, PPU addr hi/lo, 32 tiles, $FF terminator) for frame $040D of slot $040C; PPU addr = CardRowBaseTable[slot mod 3 via B1F_MathDiv16] + frame*$20; patterns from CardAnimPatternPtrs ($B385, 10 frames, 8/9 share $B499); id $FE selects shared frame 8; sets addr_nmi_ctrl ($007E) bit2 = NMI VRAM-buffer-write request consumed by prg_1f NmiDoBank3D_VramWrite $EEBE -> B1D_1E_VRAMBufferWrite ($A11B); steps $040D 0-9 then $FF. CardFillDispatch: frame-dispatched Officer-specific fill (0 name marks, 1 base kana, 3 stats, 4/5 portraits+stats, 6/7 name plates producing $040E/$040F marker coords, 8 marker commit to $00BC/$00BD gated on sub-state 3, 2/9 noop); skips pending-transfer ids >= $ED; record ptr ($10) = id*12+$63C0 via B1F_GetOfficerRecordAddr. Also: functions.h gained B19_1A_OfficerCardAnimStep_Entry = $A012; six cross-bank .word $A012 trampoline targets (prg_0c_0d $A265/$C39F, prg_17_18 $BC70/$DA46/$DABB, prg_08_09 $D940 — all LDY #$39 = banks $19+$1A) converted to the symbolic name. Note: full `make` has ~60 pre-existing duplicate-symbol errors (prg_1b_1c vs prg_19_1a Loc_ labels) and a missing prg_0b.asm include; per-bank harness is the authoritative check.
